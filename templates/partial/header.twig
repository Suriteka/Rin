{% block header %}
    <header class="header">
        <a class="header__link" href="{{site.url}}" rel="home">
            <img class="header__logo" src="{{ site.icon.src }}" alt="Logo">
        </a>
    </header>
{% endblock %}